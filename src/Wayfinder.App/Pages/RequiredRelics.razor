@page "/required-relics"

@using Microsoft.Extensions.DependencyInjection;
@using Wayfinder.App.Features.RequiredUnits;

@inherits OwningComponentBase

<RequiredUnits Title="Required Relics" ViewModel="@ViewModel" />

@code {
    private RequiredUnitsViewModel ViewModel { get; set; } = default!;

    protected override void OnInitialized()
    {
        ViewModel = ScopedServices.GetRequiredService<RequiredRelicUnitsViewModel>();
    }
}